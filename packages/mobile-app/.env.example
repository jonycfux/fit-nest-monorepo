# Baked into the app binary at build time (changing it requires a rebuild).
# Dev default if unset: http://10.0.2.2:4000 (Android) / http://localhost:4000 (iOS).
# Physical device: use your machine's LAN IP, e.g. http://192.168.1.20:4000
EXPO_PUBLIC_API_URL=http://localhost:4000
